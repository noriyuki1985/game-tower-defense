## v4.6.7
- 画像生成なし
- 敵が被弾時のノックバックで経路外へ押し出され、攻撃対象外になる問題を修正。
- 敵の `x/y` は経路進行の正位置として固定し、被弾揺れは描画時の一時オフセットに限定。
- `VERSION` を `v4.6.7`、`SAVE_KEY` を `kbd-v467-save` に更新。

## v4.6.6
- 画像生成なし
- スマホで画像読込完了前にステージ開始した場合、読込完了後の boot 処理でゲーム状態 `S` が `null` に戻される問題を修正。
- 開始オーバーレイだけ消えて、マップ背景は出るが主人公・敵・施設が出ない状態を防止。

## v4.6.5
- 画像生成なし
- スマホ版でステージ選択プルダウンが空表示になる問題を修正。
- HTML初期状態に `meadow` のフォールバック option を追加し、画像読込完了前でも選択値が空にならないようにした。
- `ensureStageSelectReady()` / `safeStageId()` を追加し、開始時に select が空でも解放済みステージへ安全にフォールバックするよう修正。
- アセット読込完了前にもステージ選択UIを事前初期化するように変更。

## v4.6.4
- 主人公 idle / run / bow attack / hit の見た目確認を行い、bow attack の位置合わせを最終調整。
- bow attack 4フレームを160x160の同一キャンバスへ再配置し、足元位置を他アニメと統一。

## v4.6.3
- 主人公 bow attack 4フレームを再生成ベースで再実装。
- 生成画像のチェッカーボード背景を除去し、完全透明背景の attack スプライトシートへ再構成。
- 弓まわりの白焼き付きが出ていた旧攻撃アニメを差し替え。

## v4.6.2
- 画像生成なし
- 弓塔 / 大砲 / 兵舎 / 柵 / 採掘所の描画倍率を見直し、主人公・敵と並んだ時の存在感を強化。
- 建物ごとの基準高さと描画倍率を定数化し、今後の画像差し替えでも調整しやすい構成へ整理。

## v4.6.1
- 画像生成なし
- 主人公スプライト一式（idle / run / bow attack / hit）の半透明ピクセルから白マット由来のフチを除去。
- 主人公の待機 / 移動 / 攻撃 / 被弾描画を、トリミング基準ではなく同一キャンバス基準の下端揃え描画へ変更。
- 主人公の弓攻撃時に、専用4フレーム bow attack シートを再び使うよう修正。

## v4.5.3
- Fixed opaque white background on tower attack sprite sheets.
- Replaced the hero bow attack sprite switch with a stable base-pose attack cue to remove scale/style mismatch.
- Reduced ally hit whitening by using a subtle outline cue instead of additive full-sprite redraw.


## v4.5.1
- ユーザー提供のデバッグ済み `index.html` を統合。
- Canvas入力イベントを `PointerEvent` 優先に整理し、マウス/タッチの多重発火を抑制。
- オーバーレイ表示をスクロール可能にし、スマホ縦画面で開始/設定UIが詰まる問題を軽減。
- メインループに例外ガードを追加し、1フレームの描画/更新エラーでゲーム全体が停止しないように調整。


## v4.4.2
- 弓塔の発射4フレームを画像生成し、攻撃時に専用アニメーションへ切替。
- 大砲の発射4フレームを画像生成し、点火→発射→煙の流れを反映。
- 兵舎の出撃/召喚演出を追加。兵士出現時に光輪・エフェクト・発生パルスを表示。

# Changelog

## v4.3.9 - Ally spear move / attack / battle FX polish
- 味方槍兵の移動4フレーム画像を生成し、ゲーム内の移動アニメに反映
- 味方槍兵の攻撃4フレーム画像を生成し、ゲーム内の攻撃アニメに反映
- 味方兵の待機/移動/攻撃の切り替えを調整
- 矢の軌跡を強化し、槍突きのスラスト感と被弾フラッシュ演出を改善
- SAVE_KEY を `kbd-v439-save` に更新

## v4.3.6 - King idle / run animation unify
- 画像生成あり
- 主人公の待機4フレーム画像を新規生成し、既存の旧絵柄を差し替え
- 主人公の移動4フレーム画像を新規生成し、弓攻撃 / 被弾と絵柄を統一
- 待機アニメを4フレーム循環へ変更
- セーブキーを `kbd-v436-save` に更新

## v4.3.5 - King hit reaction animation
- 画像生成あり
- 主人公の4フレーム被弾スプライトシートを追加
- 敵接触時に主人公の被弾リアクションと短いフラッシュを再生
- 主人公の被弾中は一瞬だけ攻撃を中断し、リアクションが見えるように調整

## v4.3.4 - King bow attack animation
- 画像生成あり
- 主人公の攻撃を近接斬撃から短距離の弓攻撃へ変更
- 王の4フレーム弓攻撃スプライトシートを追加
- 攻撃予備動作のあとに矢プロジェクタイルを発射する方式へ変更
- 主人公攻撃射程を延長し、少し離れた敵にも対応

## v4.3.3 - Enemy attack / hit / siege destruction animations
- 画像生成あり
- 敵攻撃用の4フレームアニメーションシートを追加し、接敵中は歩行ではなく攻撃モーションを再生
- 被弾時に短い硬直を追加し、白フラッシュを強化
- 攻城車撃破時に専用の破壊スプライト演出を追加

## v4.3.1 - Enemy walk animation sheets

- Generated and added 4-frame walk sprite sheets for all enemy types.
- Added `assets/images/enemies/walk/` as the enemy animation asset directory.
- Switched enemy rendering from static enemy PNGs to frame-based walk sheets.
- Added movement-speed-based frame advancement: runners animate faster, siege engines animate slower, boss enemies move heavily.
- Kept static enemy images as fallback assets.
- No external communication code added.


## v4.3.0 - Stage select map and progression

- Changed the initial screen into a dedicated stage-select map.
- Added stage unlock progression: clearing a stage unlocks the next stage.
- Added cleared/unlocked stage persistence via localStorage.
- Added next-stage flow on the victory screen.
- Added stage-select return and same-stage retry buttons on the result screen.
- Added a progress reset button on the stage-select screen.
- Kept this as a code-only update; no image assets were added.

# CHANGELOG

## v4.2.0 — 新ステージ3種追加

- `rivergate` / 橋門の前線 を追加。橋に敵が集中する3ルート構成で、柵と大砲の価値を確認しやすいステージ。
- `forest` / 森陰の前線 を追加。曲がり道と高速敵が多く、王の移動・兵舎・弓塔配置を試すステージ。
- `bastion` / 廃砦の前線 を追加。2ルートだが重兵・破城槌・盾兵が多く、火力不足を診断しやすい高難度ステージ。
- 開始画面・設定メニューのステージ選択に新3ステージを自動表示。
- 各新ステージにルート、建設スロット、ウェーブ構成、初期資金、敵倍率、診断表示を追加。
- 背景色と簡易地形演出をステージ別に追加。画像生成・画像追加はなし。

## v4.1.0 — ステージ選択復帰・診断表示再統合

- v4コアに `meadow` / `crossroads` / `ridge` のステージ選択を再統合。
- ステージごとのルート、建設スロット、ウェーブ構成、初期資金、敵倍率を切り替え可能にした。
- 開始画面にステージ選択、短い概要、診断値（初期資金、敵倍率、建設床数、ルート別敵数）を表示。
- 設定メニューにもステージ選択を追加し、「最初からやり直す」で選択ステージを反映できるようにした。
- 背景色とルート描画をステージ別に切り替え、v4の単一HTML構成を維持したまま複数ステージ運用へ戻した。

## v4.0.5 — 設置メニュー誤操作対策

- 建設メニューのボタンサイズを拡大し、PC/スマホ両方で押しやすく調整。
- 建設メニュー全体に安全領域を設け、メニューボタン付近のタップ/クリックが移動指示に化けにくいよう修正。
- 建設・強化・修理直後の短時間入力を無視し、同一操作の誤爆を抑制。
- 金不足時のメッセージを「金不足」に短縮し、表示時間も短くした。
- 建設メニューの表記を短くし、情報密度を下げた。


## v4.0.2 — 主人公攻撃復旧・改善提案反映

- v4.0.1 のコア再設計後、主人公の近接攻撃処理が存在しなくなっていた問題を修正。
- `BAL.kingAttackRange` / `kingAttackDamage` / `kingAttackCd` / `kingAttackWindup` を追加し、王の自動斬撃をバランス調整しやすい定数へ集約。
- 王の攻撃時に向き補正、斬撃表示、バースト、リング、浮き文字を追加。
- `damageEnemy()` に source 引数を追加し、王の攻撃は旧仕様同様に装甲軽減を受けにくい処理にした。

## v4.0.1 — 起動バグ修正

- ページ読込時、ゲーム開始前から描画ループが回り始め、状態 `S` が `null` のまま `update()` が `S.status` を参照して初回フレームで例外発生 → ループが停止し、「出陣する」を押しても何も起きない不具合を修正。
- 対応：`update()` を null セーフ化（`if(!S || S.status!=="playing") return;`）し、ループ側も `S` が存在する時だけ更新するよう変更。起動→アイドル→開始→実行まで例外ゼロを確認。

## v4.0.0 — コア再設計（core rebuild）

v3.12.4 で感じていた「ゲームとしての物足りなさ」を、表面の調整ではなくコアの仕組みから作り直した版。
単一ファイル（`index.html`）構成へ移行し、既存アセットを流用。

### 設計の作り直し

- **建物配置を「役割タグ付き離散スロット」に変更**
  道スロット＝柵のみ／高所スロット＝弓塔・大砲・兵舎／金鉱スロット＝金鉱のみ。
  → 「道の真ん中に弓塔が建つ」問題を構造的に解消。塔は必ず道を見下ろす位置に座る。

- **お金の仕組みを刷新（連続吸い上げを廃止）**
  スロットでアクションを選ぶと費用を **その場で一括消費**。残高不足なら実行不可。
  建設／強化／修理を1回行うとそのスロットは **ロック**し、王が離れて戻るまで再操作できない。
  → 「レベルアップしたら一旦お金を使うのをやめる」を仕組みとして実装。立ちっぱなしで財布が溶ける挙動を撤廃。

- **経済と戦闘の択を作った**
  金鉱はウェーブ突破後に一括収入。早い投資ほど後半が伸びる。ただし前線から離れた3か所にしか建たず、建設／強化の間は主戦場を空ける（機会費用）。
  弓塔・大砲は王が近いと発射が速くなる。前線に立つか、後方で経済を回すか――王の位置取りが戦略になる。
  強化費は段階ごとに逓増（×1.6）するため全施設をMAXにはできず、毎ウェーブ投資先を選ぶ。

- **マップを一新**
  2ルート（主街道・側道）が中央で合流して城へ向かう構成。合流点が自然な殺し間になる。
  背景は手続き描画（草グラデ＋深度ブロブ＋柔らかい道＋木・岩＋ドリフトする霧）で、王や建物のトゥーン絵柄と画風を統一。単調さを解消。

### 演出

- レベルアップ＝ヒットストップ＋段階色の衝撃波リング＋スプライトのスウィープ切替＋粒子＋画面フラッシュ＆シェイク＋上昇音。Lv4は専用バーストと持続グロー。
- 王＝移動方向へのリーン／スクワッシュ＆ストレッチ／足元の砂煙、待機の呼吸、建設ポーズ。
- 敵＝歩行ボブ／被弾フラッシュ＆ノックバック／撃破バースト／コインドロップ。
- 兵士＝攻撃ランジ（攻撃フレーム流用）。

### コンテンツ

- 建物5種（柵・弓塔・大砲・兵舎・金鉱）×各4レベル（既存Lv1-4画像を流用）。
- 敵10種を流用、8ウェーブの難易度アーク。最終ウェーブは将軍ボス。
- localStorage に最高到達ウェーブと音設定のみ保存。**ゲーム内通信ゼロ**（詳細は README）。

### 確認済みの既知メモ

- 数値（コスト・収入・HP・ウェーブ物量など）は初期値。実プレイ後の調整を前提に `BAL` / `STAT` / `DEF` / `WAVES` に集約してある。
- 高所スロットは道から 46〜74px 脇に配置（塔の射程 132〜158 で道を確実にカバー）。道スロットはルート上（0.6〜3.3px）に配置し柵が確実に食い止める。
- `file://` 直開きは画像が出ない場合があるため、ローカル確認はサーバ経由を推奨（README参照）。

## v4.5.3 - Sprite scale normalization debug
- 主人公の待機 / 移動 / 攻撃 / 被弾アニメーションで、透明余白の差による見た目サイズのブレを抑制。
- 敵キャラクターの表示高さを種類別に再定義し、主人公・一般敵・大型敵・ボスの画面上スケールを整理。
- 描画時に画像/フレームごとの不透明ピクセル範囲をキャッシュして切り出す `spriteTrim` / `spriteFrameTrim` を追加。
- 味方ユニットも同じトリム描画に寄せ、槍兵・弓兵のサイズ感を安定化。
